{
  "name": "devlens",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "demo",
    "demo-vanilla"
  ],
  "scripts": {
    "dev": "npm run dev --workspace devlens-demo",
    "dev:vanilla": "npm run dev --workspace devlens-demo-vanilla",
    "build": "npm run build --workspace devlens-demo",
    "build:vanilla": "npm run build --workspace devlens-demo-vanilla",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
